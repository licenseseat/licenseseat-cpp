@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
# OpenSSL is used by cpp-httplib for HTTPS, not by licenseseat directly

include("${CMAKE_CURRENT_LIST_DIR}/licenseseatTargets.cmake")

check_required_components(licenseseat)
